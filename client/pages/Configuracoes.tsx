import { Settings } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function Configuracoes() {
  return (
    <PlaceholderPage
      title="Configurações"
      description="Configure parâmetros do sistema, usuários e preferências do atendimento clínico"
      icon={Settings}
    />
  );
}
